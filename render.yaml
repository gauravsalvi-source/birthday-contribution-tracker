services:
  - type: web
    name: birthday-tracker
    runtime: node
    buildCommand: npm run build
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        value: ""
