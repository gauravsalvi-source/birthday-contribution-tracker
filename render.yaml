services:
  - type: web
    name: birthday-tracker
    runtime: node
  installCommand: npm install --include=dev
    buildCommand: npm run build
    startCommand: node server/server.js
postDeployCommand: node server/seedMay2026.js
    envVars:
      - key: DATABASE_URL
        value: "${DATABASE_URL}"
